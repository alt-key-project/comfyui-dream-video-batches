# Prompt Construction Nodes

| Field | Value |
|-------|-------|
| **ID** | TC-021 |
| **Type** | node |
| **Features** | FT-007 |

## Code Locations
- `prompting.py`

## Exported Symbols
- `RandomPromptScheduleGenerator` — 随机提示词调度生成器
- `DreamWeightedPromptBuilder` — 加权提示词构建器
- `DreamPromptFinalizer` — 提示词最终化节点

## Description

基于 `PartialPrompt` 数据结构的提示词构建系统：

- **PartialPrompt** (TC-007): 核心数据结构，以字典 `{text: weight}` 存储加权提示词片段。支持 `add()`, `merge()`, `random_subset()`, `scaled_by()`, `finalize()`, `finalize_signed()`
- **DreamWeightedPromptBuilder**: 将文本转换为带权重的 PartialPrompt 片段
- **DreamPromptFinalizer**: 将 PartialPrompt 转为最终正/负提示词字符串，支持 raw/by_abs_max/by_abs_sum 调整
- **RandomPromptScheduleGenerator**: 生成逐帧变化的提示词调度表，用于 AnimateDiff 等视频生成工作流

*Auto-generated by codebase-rebuild at 2026-05-20T00:00:00Z*
