# FrameSet

| Field | Value |
|-------|-------|
| **ID** | TC-001 |
| **Type** | core |
| **Features** | FT-001, FT-002 |

## Code Locations
- `core/frameset.py`

## Exported Symbols
- `FrameSet` — 带索引的图像批处理核心数据结构，包含 tensor、framerate、indices
- `IndexedImage` — 图像 + 帧索引的配对数据结构

## Description

FrameSet 是整个节点包的数据中枢。每个 FrameSet 包含：
- 图像 tensor（来自 ComfyUI 的 IMAGE 类型）
- 帧率（FrameRate，base/divisor 形式）
- 帧索引列表（整数列表，支持非连续索引）

主要操作：
- `reindexed(first_index, step)` — 重新索引帧
- `merge(other)` — 合并两个 FrameSet（优先级：已有帧覆盖新帧）
- `fade_to(frames_after, fade_length)` — 渐变过渡到另一个 FrameSet
- `generate_inbetween_blended/closest/previous()` — 补间帧生成
- `images` / `tensor` / `indexed_images` / `indexed_length` — 只读属性
- `from_images()` / `from_indexed_images()` — 工厂方法

*Auto-generated by codebase-rebuild at 2026-05-20T00:00:00Z*
