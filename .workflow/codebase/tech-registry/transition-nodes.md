# Transition Nodes

| Field | Value |
|-------|-------|
| **ID** | TC-022 |
| **Type** | node |
| **Features** | FT-004 |

## Code Locations
- `transitions.py`

## Exported Symbols
- `DVB_FadeToBlack` — 淡出到黑色
- `DVB_FadeFromBlack` — 从黑色淡入
- `DVB_BlendedTransition` — 两个 FrameSet 之间的渐变过渡
- `FadeProc` — 亮度渐变处理器

## Description

视频过渡节点集：

- **FadeProc**: 内部类，通过 `DVB_Image.change_brightness()` 在指定帧数内线性改变亮度
- **DVB_FadeToBlack / DVB_FadeFromBlack**: 接收 FrameSet + fade_seconds，内部计算 fade_length（帧数），使用 `DVB_ImageBatchProcessor` 批量执行
- **DVB_BlendedTransition**: 将两个 FrameSet 交叉淡入淡出，包含帧率一致性和尺寸一致性验证，重叠区域通过 `DVB_Image.blend()` 逐帧混合

*Auto-generated by codebase-rebuild at 2026-05-20T00:00:00Z*
