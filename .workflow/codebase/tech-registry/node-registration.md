# Node Registration & Manifest

| Field | Value |
|-------|-------|
| **ID** | TC-013 |
| **Type** | entry |
| **Features** | FT-012 |

## Code Locations
- `__init__.py`

## Exported Symbols
- `NODE_CLASS_MAPPINGS` — ComfyUI 节点名 → 类映射
- `NODE_DISPLAY_NAME_MAPPINGS` — ComfyUI 显示名映射
- `MANIFEST` — 节点包元信息
- `config` — DVB_Config 全局实例

## Description

项目入口文件，负责：

1. **注册所有节点** — `_NODE_CLASSES` 列表包含 28 个活跃节点类
2. **UI 配置** — 通过 `DVB_Config` 读取 `config.json`，动态设置：
   - 类别图标（prepend/append）
   - 节点图标
   - 顶层类别名称（默认 "DVB"）
3. **签名后缀** — 所有节点名追加 ` [DVB]` 标识
4. **节点列表管理** — `update_node_index()` 增量更新 `node_list.json`

*Auto-generated by codebase-rebuild at 2026-05-20T00:00:00Z*
